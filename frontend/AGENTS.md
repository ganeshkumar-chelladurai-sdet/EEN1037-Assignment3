# AGENTS.md

This project is a React + Vite frontend, that connects to the Django REST API backend in ../backend.

* Install dependencies: `npm install`
* Dev server: `npm run dev`
* Run tests: `npm test`

See [DEVELOPMENT.md](DEVELOPMENT.md) for detailed setup and testing instructions.
